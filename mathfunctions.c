#include <stdio.h>
#include <math.h>

int main()
{

    float x = 3.14;
    // x = sqrt(x);

    // x = pow(x, 3);

    // x = round(x); rounds depends on the number

    // x = ceil(x); always runs up

    // x = floor(x); rounds always down

    // x = abs(x); Absoulte value function
    printf("%.2f", x);

    return 0;
}